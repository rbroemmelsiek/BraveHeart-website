# 00. Brave Heart Source Normalization

⚠️ **Medium** — Requires careful reconciliation of source copy, old assets, and cloned listing-site structure.

## Objective

Inventory and normalize Brave Heart source materials before application code changes.

## Repo Inspection Preconditions (2026-06-21)

- Listing-site routes and shell are intact in `src/`; no Brave Heart routes implemented yet.
- Brave Heart images exist at **`public/assets/`** (16 PNG + 3 WebP), not yet at the planned production path `public/assets/images/braveheart/`. Inbox contains only `.gitkeep`.
- Listing-site page images reference **missing root-level paths** (e.g. `/patio-pool.webp`); these files are not in the repo.
- Normalized content under `docs/braveheart/normalized-content/` is **draft outline only** — not ready for React implementation without approval expansion.
- Reference material lives in `docs/reference-listing-site/` (not `docs/reference/Stitch Luxury Home Listing Desktop/`).

## Scope

- Inspect current repo structure.
- Identify existing listing-site routes/components.
- Inventory Brave Heart images (on-disk and planned).
- Reconcile target routes.
- Create/update content map, asset inventory, and implementation source of truth.
- Complete `.agent/notes/00.repo-and-source-inspection.md`.

## Out of Scope

- No React code changes.
- No styling changes.
- No route implementation.
- No WebP conversion (deferred to plan 03).

## Files Likely Affected

- `docs/braveheart/asset-inventory.md`
- `docs/braveheart/content-map.md`
- `docs/braveheart/image-conversion-report.md` (status notes only)
- `docs/braveheart/route-map.md` (if mismatches found)
- `.agent/notes/00.repo-and-source-inspection.md`
- `PROGRESS.md` (step 00 status)

## Tasks

1. Read root docs: `README.md`, `PRD.md`, `CLAUDE.md`, `PROGRESS.md`.
2. Inspect `package.json`, `src/App.tsx`, `src/pages/*`, `src/components/*`, `src/index.css`.
3. Inventory **both**:
   - `public/assets/` (current on-disk Brave Heart PNG/WebP files)
   - `public/assets/images/braveheart/` and `inbox/`
4. Reconcile on-disk filenames (including spaced names like `Brave Heart Logo 2.png`) against `docs/braveheart/asset-inventory.md` and `docs/source/braveheart/braveheart_image_optimization_manifest.md`.
5. Register six additional on-disk assets not yet in inventory: `automated-escrow-title-closing-precision`, `braveheart-legacy-of-support-history`, `comprehensive-lifecycle-real-estate-benefits`, `talentscout-ai-agent-matching-system`, `braveheart-app-qr-code-signup`, `icon-final`.
6. Mark `Karen2-250px.webp` as listing-site legacy / retire.
7. Document listing-site broken image paths (root `/ *.webp` references with no files).
8. Update `docs/braveheart/asset-inventory.md` with accurate status values.
9. Update `docs/braveheart/content-map.md` — flag all sections as `draft` / `needs-review` until copy is expanded.
10. Update `.agent/notes/00.repo-and-source-inspection.md` with all 14 inspection sections.

## Validation

- All current listing routes documented.
- All target Brave Heart routes documented.
- Route slug mismatches documented (`/privacy` vs `/privacy-policy`, `/terms` vs `/terms-of-service`).
- Existing shared components documented.
- On-disk Brave Heart assets reconciled with asset inventory (including extras and gaps).
- Listing-site missing image paths documented.
- Content map reflects draft status of normalized content.
- Asset inventory exists and matches repo reality.
- Inspection note marked complete.
- No application code modified.

## Rollback Notes

Documentation-only plan. Revert markdown edits via git if reconciliation is incorrect. No runtime impact.

## Risks Before Execution

- Asset inventory may drift again if images are added outside the inbox workflow.
- Normalized content appears ready by filename but body copy is not — risk of premature implementation in plan 02.
- Filename inconsistencies (spaces, mixed PNG/WebP) may cause broken paths if not normalized before plan 03.

## Approval Gate

Stop after documentation updates and wait for approval before plan 01.
