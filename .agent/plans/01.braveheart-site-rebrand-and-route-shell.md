# 01. Brave Heart Site Rebrand and Route Shell

⚠️ **Medium** — Converts existing route shell while preserving architecture.

## Objective

Convert cloned listing-site routes and global shell into Brave Heart routes and brand identity.

## Repo Inspection Preconditions (2026-06-21)

- Current routes: `/`, `/residence`, `/neighborhood`, `/schools`, `/gallery`, `/privacy`, `/terms`, `/accessibility`.
- Navbar/footer still show **1622 W. Oak St.** listing branding and listing nav links.
- Legal pages use listing copy but reusable layout patterns (numbered sections, reveal animations).
- No per-page SEO metadata exists yet (single static `<title>` in `index.html`) — defer full metadata to plan 05; update `index.html` title only here.
- Do **not** delete listing page files until all target routes render.

## Scope

- Update `index.html` app title (and favicon reference if logo asset approved).
- Replace navigation labels and brand text in Navbar/Footer.
- Add target Brave Heart routes with stub page components.
- Preserve existing Navbar/Footer/ContactModal/ScrollToTop patterns.
- Add redirects or `<Navigate>` fallbacks for old listing routes and old legal slugs.
- Remove old property routes from primary nav (keep files until stubs verified).

## Out of Scope

- Full page copy (plan 02).
- Final image insertion (plan 03).
- Per-route SEO metadata (plan 05).
- Verification backend.
- CMS/blog.
- ContactModal content overhaul (plan 04 — may update CTA label only).

## Files Likely Affected

- `index.html`
- `src/App.tsx`
- `src/components/Navbar.tsx`
- `src/components/Footer.tsx`
- `src/pages/Home.tsx` (stub only — or replace with new `Home.tsx` stub)
- **New:** `src/pages/OurStory.tsx`, `WhoWeServe.tsx`, `SavingsComparison.tsx`, `ServiceLines.tsx`, `OurGuarantee.tsx`, `CaseStudies.tsx`
- **Rename or duplicate:** legal pages to match `/privacy-policy`, `/terms-of-service` slugs
- `.agent/validation/braveheart-site-validation.md`
- `PROGRESS.md`

## Target Routes

```text
/
/our-story
/who-we-serve
/savings-comparison
/service-lines
/our-guarantee
/case-studies
/privacy-policy
/terms-of-service
/accessibility
```

## Legacy Route Handling

Add redirects (React Router `<Navigate replace />` or equivalent) for:

| Old route | Action |
|---|---|
| `/residence` | Redirect to `/our-story` (or 404 until content mapped) |
| `/neighborhood` | Redirect to `/who-we-serve` |
| `/schools` | Redirect to `/savings-comparison` |
| `/gallery` | Redirect to `/case-studies` |
| `/privacy` | Redirect to `/privacy-policy` |
| `/terms` | Redirect to `/terms-of-service` |

Confirm redirect targets with owner if page mapping differs.

## Tasks

1. Create stub route components for all seven new Brave Heart pages (route-specific H1 + minimal placeholder section).
2. Update `src/App.tsx` route configuration for target routes and legacy redirects.
3. Update Navbar links to target routes; replace listing brand with Brave Heart First Responders.
4. Update Footer links to `/privacy-policy`, `/terms-of-service`, `/accessibility`; replace listing copyright.
5. Update `index.html` `<title>` to Brave Heart (default site title).
6. Update Navbar CTA button label (e.g. "Verify Eligibility") — wire behavior in plan 04.
7. Keep listing page files in repo but unreachable from nav (redirects cover direct URL access).
8. Update validation report route table.

## Validation

- All 10 target routes render without console errors.
- Navbar links resolve to target routes.
- Footer legal links resolve to `/privacy-policy`, `/terms-of-service`, `/accessibility`.
- No listing property route appears in primary nav.
- Legacy routes redirect (manual test each old URL).
- `npm run build` passes.
- `npm run lint` (`tsc --noEmit`) passes.
- Mobile nav opens, closes on route change, and new links work.

## Rollback Notes

Revert `App.tsx`, Navbar, Footer, and new page files. Restore listing routes in nav. Listing page files remain if not deleted.

## Risks Before Execution

- Creating seven stub pages in one pass is feasible but large; typos in route paths will break nav.
- Legal slug change breaks bookmarked `/privacy` and `/terms` unless redirects are added.
- Premature deletion of listing pages violates CLAUDE.md — keep until stubs verified.
- ContactModal still contains listing agent contact info until plan 04.

## Approval Gate

Stop before full content insertion (plan 02).
