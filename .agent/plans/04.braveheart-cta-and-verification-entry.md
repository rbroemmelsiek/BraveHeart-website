# 04. Brave Heart CTA and Verification Entry

⚠️ **Medium** — Adds conversion paths without claiming live backend verification.

## Objective

Implement primary and secondary CTA flows for verification, benefits, founder contact, and app entry.

## Repo Inspection Preconditions (2026-06-21)

- **Depends on plans 01–02** — pages and nav must exist; content sections should include CTA blocks.
- `ContactModal.tsx` contains **listing-site contact data**: `karen@vannessandassociates.com`, `+18586996409`, "1622 W. Oak St., Burbank".
- `Home.tsx` includes an inline mailto form using the same listing agent email — must be replaced or removed in plan 02/04.
- Navbar CTA currently says "Schedule Tour" — rebrand label may start in plan 01; behavior wired here.
- CTA destinations are **open decisions** in `PROGRESS.md` — stub links required until confirmed.
- Reference: `docs/braveheart/cta-and-verification-flow.md`.

## Scope

- Reuse or refactor `ContactModal` and `ContactModalContext` pattern.
- Add Verify Eligibility primary CTA (navbar, hero, key pages).
- Add See Benefits / Learn More secondary CTAs per content map.
- Add Speak With Founder CTA.
- Add Open App / QR placeholder if final route unknown (`braveheart-app-qr-code-signup` asset exists).
- Document all stubbed links in `cta-and-verification-flow.md`.
- Remove all listing-agent contact references from public UI.

## Out of Scope

- Verification backend or eligibility API.
- Authentication or account creation.
- CRM integration.
- Payment or billing.
- Third-party verification service integration.

## Files Likely Affected

- `src/components/ContactModal.tsx`
- `src/components/Navbar.tsx` (CTA onClick / href)
- `src/pages/Home.tsx` (hero CTA, remove/replace mailto form)
- Other plan 02 page files (CTA blocks)
- `docs/braveheart/cta-and-verification-flow.md`
- Optional: `src/config/cta.ts` or `docs/braveheart/white-label-config.md` for centralized CTA URLs
- `.agent/validation/braveheart-site-validation.md`
- `PROGRESS.md`

## Tasks

1. Define CTA config (URLs, labels, stub flags) — use placeholders from `white-label-config.md` where available.
2. Update `ContactModal` copy and actions for Brave Heart (verify, contact founder, app entry).
3. Replace listing email/phone/SMS with approved Brave Heart contact or stubbed `#` / `mailto:` pending approval.
4. Wire Navbar CTA to primary action (modal or external verify URL).
5. Wire page-level CTAs per content map across all Phase 1 routes.
6. Add visible stub indicator in dev/docs for non-live verification/app links (not in production UI — document only).
7. Remove Home page listing mailto form or repurpose for founder contact with approved destination.
8. Update `cta-and-verification-flow.md` with final/stubbed state.

## Validation

- All CTA buttons and links are clickable (no dead `onClick` handlers).
- Stubbed CTAs documented in `cta-and-verification-flow.md` with owner-visible status.
- No listing-agent contact info remains (grep: `vanness`, `86996409`, `Karen`).
- No CTA text guarantees approval, savings amounts, or eligibility before verification.
- Contact links (`mailto:`, `tel:`) use approved destinations or documented stubs.
- Mobile: Navbar CTA and modal actions reachable and usable.
- `npm run build` passes.
- `npm run lint` passes.

## Rollback Notes

Revert ContactModal and CTA href changes. Restore previous modal content if needed. CTA config file can be deleted independently.

## Risks Before Execution

- Wiring real verification URLs before provider is chosen creates rework.
- Home mailto form removal may reduce contact paths until founder contact is approved.
- Modal-only CTAs may be insufficient for "Verify Eligibility" if external app URL is required.

## Approval Gate

Stop before adding any third-party verification service. Stop before publishing real contact PII without owner approval.
